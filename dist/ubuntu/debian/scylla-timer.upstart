description "A timer job file for running scylla-housekeeping"

start on started scylla-server
stop on stopping scylla-server

script
  while [ 1 ]
  do
    /usr/lib/scylla/scylla-housekeeping -q version || true
    sleep 1m
  done
end script